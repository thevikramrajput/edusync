[build]
builder = "NIXPACKS"

[build.nixpacks]
plan = "python"

[deploy]
startCommand = "gunicorn edusync.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --timeout 120"
healthcheckPath = "/api/v1/health/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
